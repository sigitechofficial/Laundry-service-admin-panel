# DEPRECATED — replaced by Careflow-style `.github/workflows/deploy-admin.yml`
name: Legacy Admin Deploy (DEPRECATED)

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Type USE_LEGACY_ADMIN to acknowledge'
        required: true
        type: string

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - run: |
          if [ "${{ github.event.inputs.confirmation }}" != "USE_LEGACY_ADMIN" ]; then
            echo "Use Actions → Deploy Laundry Admin"
            exit 1
          fi
          echo "Legacy Next/PM2 git-pull deploy is retired. Use deploy-admin.yml"
          exit 1
